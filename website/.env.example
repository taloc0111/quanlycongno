# URL gốc của backend API. Để trống khi chạy local (mặc định http://localhost:5000/api).
# Khi deploy: đặt thành URL backend trên Render, ví dụ:
#   VITE_API_URL=https://your-app.onrender.com/api
VITE_API_URL=
